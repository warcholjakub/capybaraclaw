// Capabilities preamble (loaded first via the `requires` declaration) has
// already imported `safemode.lib.{*, given}`. Functions defined by this
// plugin live in the same package, so wildcard import covers them too —
// no further preamble work needed here.
