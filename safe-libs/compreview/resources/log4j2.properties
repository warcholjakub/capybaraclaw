# Silent Log4j2 config. POI 5.x logs via Log4j2 API; with this config on the
# classpath the provider initializes quietly and emits nothing — keeps the
# REPL output clean for the agent.
status = error
name = SilentConfig

rootLogger.level = off
